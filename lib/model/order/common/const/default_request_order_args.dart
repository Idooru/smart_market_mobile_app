import '../../domain/entities/order.entity.dart';

const RequestOrders defaultRequestOrdersArgs = RequestOrders(
  align: "DESC",
  column: "createdAt",
  deliveryOption: "none",
  transactionStatus: "none",
);
